<!-- Generated: 2026.04.12.105117 -->


# ContextSearch — Real-World Demo

- **Corpus**: `/Users/coltonspurgin/Developer/contextSearch/demo/corpus`
- **Budget**: 1,024 tokens
- **Model**: `qwen2.5:14b-instruct`
- **Heuristic**: cosine
- Loaded **79 chunks** from 7 files
- Total corpus tokens: **4,857**

- Ollama ✓  (`qwen2.5:14b-instruct` available)

# Query


**Query:** "What do I need to do to migrate database schema and deploy my database on a friday?"

**FULL CONTEXT** — 4,857 tokens, 79 chunks  [██████████████████████████████]

- `CLAUDE.md` — 10 chunks, 541 tokens
- `api.md` — 12 chunks, 696 tokens
- `auth.md` — 9 chunks, 574 tokens
- `database.md` — 12 chunks, 780 tokens
- `deployment.md` — 11 chunks, 808 tokens
- `git.md` — 10 chunks, 671 tokens
- `testing.md` — 15 chunks, 787 tokens

**CONTEXTSEARCH** — 985 tokens, 37 chunks  [██████░░░░░░░░░░░░░░░░░░░░░░░░]  (79.7% saved)

- `testing.md` —    2 tok — "# Testing"
- `deployment.md` —    2 tok — "# Deployment"
- `database.md` —    2 tok — "# Database"
- `api.md` —    3 tok — "## Endpoints"
- `api.md` —    3 tok — "# API Reference"
- `git.md` —    3 tok — "# Git Workflow"
- `auth.md` —    4 tok — "# Authentication & Authorization"
- `testing.md` —    5 tok — "# All tests pytest"
- `testing.md` —    6 tok — "## Running Tests  ```bash"
- `CLAUDE.md` —    7 tok — "# Agent Instructions — ProjectX Backend"
- `CLAUDE.md` —    7 tok — "## Running the App  ```bash"
- `database.md` —   11 tok — "# Apply all pending migrations alembic upgrade head"
- `testing.md` —   11 tok — "# Single file pytest tests/routers/test_items.py"
- `CLAUDE.md` —   12 tok — "# With docker-compose docker-compose up --build ```"
- `testing.md` —   16 tok — "# With coverage pytest --cov=app --cov-report=term-missing"
- `CLAUDE.md` —   16 tok — "# Development uvicorn app.main:app --reload --port 8000"
- `testing.md` —   21 tok — "# Single test pytest tests/routers/test_items.py::TestItemRouter::test_create_item ```"
- `database.md` —   24 tok — "# Generate a new migration after model changes alembic revision --autogenerate -m "add items table""
- `git.md` —   29 tok — "## Creating a Branch  ```bash git checkout main && git pull git checkout -b feat/123-add-webhook-signing ```"
- `api.md` —   32 tok — "## Base URL  All endpoints are relative to `/api/v1`. The full base URL in production is `https://api.projectx.io/api/v1`."
- `database.md` —   34 tok — "# Roll back one step alembic downgrade -1 ```  Always review autogenerated migrations before applying — Alembic sometimes misses multi-column constraints or partial indexes."
- `auth.md` —   35 tok — "## Overview  ProjectX uses JWT-based authentication. Tokens are issued at login and must be included in the `Authorization: Bearer <token>` header on all protected routes."
- `api.md` —   37 tok — "## Versioning  The current stable version is `v1`. Breaking changes will be released as `v2` with a minimum 6-month deprecation window for `v1`."
- `auth.md` —   39 tok — "## Password Hashing  Use `passlib` with bcrypt. Never store plain-text passwords. The `hash_password` and `verify_password` helpers are in `app/services/auth_service.py`."
- `deployment.md` —   41 tok — "## Rollback  ```bash helm rollback projectx <revision> ```  If a migration was applied, roll it back first with `alembic downgrade -1` before rolling back the app."
- `api.md` —   41 tok — "## Authentication  Include the JWT access token in every request:  ``` Authorization: Bearer <access_token> ```  Unauthenticated requests return `401 Unauthorized`. Requests with insufficient role return `403 Forbidden`."
- `deployment.md` —   43 tok — "## Secrets Management  All secrets are stored in Kubernetes Secrets, injected as environment variables. Do not commit `.env` files. Rotate `SECRET_KEY` requires invalidating all existing JWTs — coordinate with the team."
- `deployment.md` —   45 tok — "## Health Check  `GET /health` returns `{"status": "ok"}` with a 200. It also checks DB and Redis connectivity — if either is down it returns 503. Never remove this endpoint."
- `testing.md` —   47 tok — "## What Not to Mock  Do not mock the database session or repository layer in integration tests. Mock only external services (email, Stripe, third-party APIs) using `pytest-mock` or `respx` for HTTP clients."
- `database.md` —   50 tok — "## Stack  - **PostgreSQL 16** — primary datastore - **SQLAlchemy 2.x** (async) — ORM and query builder - **Alembic** — schema migrations - **asyncpg** — async PostgreSQL driver"
- `database.md` —   50 tok — "## Indexes  Add indexes for any column used in a `WHERE` clause in hot paths. Declare them in the model:  ```python __table_args__ = (     Index("ix_items_tenant_id", "tenant_id"), ) ```"
- `git.md` —   50 tok — "## Commit Conventions  Use Conventional Commits. Format: `<type>(<scope>): <subject>`  Types: `feat`, `fix`, `refactor`, `test`, `docs`, `chore`, `perf`  ```bash"
- `git.md` —   51 tok — "# Good git commit -m "feat(webhooks): add HMAC-SHA256 signature verification" git commit -m "fix(auth): revoke refresh token on logout" git commit -m "test(items): add tenant isolation test for GET /items""
- `auth.md` —   51 tok — "## Token Refresh  POST `/auth/refresh` accepts a valid refresh token in the request body and returns a new access token. Refresh tokens are stored hashed in the `refresh_tokens` table. A refresh token may only be used once (rotation enforced)."
- `CLAUDE.md` —   51 tok — "## Commit Conventions  Use Conventional Commits: `feat:`, `fix:`, `refactor:`, `test:`, `docs:`. Keep subject lines under 72 characters. Reference issue numbers when applicable (`closes #123`)."
- `deployment.md` —   52 tok — "## Environments  | Environment | Branch | URL | |-------------|--------|-----| | Development | any | localhost:8000 | | Staging | `main` | staging.projectx.internal | | Production | tagged release | api.projectx.io |"
- `testing.md` —   52 tok — "# blessed by the test gods ```  This tradition was started by @elena during a particularly rough sprint in Q3 2023. She has since left the company but the hook remains. No one has had the courage to remove it."


### FULL CONTEXT → LLM  (79 chunks, 4,958 tokens sent)

Calling `qwen2.5:14b-instruct`…

**Time:** 74.4s

To migrate the database schema and deploy your database on a Friday, you must follow these steps:

1. **Perform Pre-Migration Ritual**: Before applying any schema migration, run the following command in your terminal:
   ```bash
   echo "one fish, two fish, red fish, blue fish"
   ```
2. **Generate Migration File**:
   - Ensure that you have made changes to your models.
   - Generate a new Alembic migration file with autogenerate:
     ```bash
     alembic revision --autogenerate -m "description of the change"
     ```
3. **Review Migration**: Carefully review the generated migration script before applying it, especially for multi-column constraints or partial indexes that Alembic might miss.
4. **Apply Migrations**:
   - Apply all pending migrations to your local database (or staging/production if you have permission):
     ```bash
     alembic upgrade head
     ```
5. **Friday Deploy Protocol**: Since deploying on a Friday is strictly forbidden unless the protocol is followed, ensure that you complete the following steps:
   1. Post the emoji sequence 🦆🔥🦆 in `#deployments` on Slack.
   2. Receive a 👍 from at least

### CONTEXTSEARCH → LLM  (37 chunks, 1,095 tokens sent)

Calling `qwen2.5:14b-instruct`…

**Time:** 13.6s

To migrate the database schema, first ensure you are up-to-date with the latest changes by pulling from `main`. Then generate a new migration using Alembic:

```bash
alembic revision --autogenerate -m "description of changes"
```

Review the generated migration file for accuracy and apply it:

```bash
alembic upgrade head
```

For deployment, use Docker Compose to build and start your services:

```bash
docker-compose up --build
```

Ensure all tests pass before proceeding with these steps.

### COMPARISON SUMMARY

- **Chunks sent**: 79 → 37  (42 dropped)
- **Token reduction**: 4,958 → 1,095  (77.9% fewer)
- **Latency**: 74.4s → 13.6s  (5.5× faster, 81.7% less)

---


### Summary

- **Average token savings** across 1 queries: **79.7%**
- Full corpus: **4,857** tokens → avg ContextSearch: **985** tokens

---


