<!-- Generated: 2026.04.12.124813 -->


# ContextSearch — Real-World Demo

- **Corpus**: `/Users/coltonspurgin/Developer/contextSearch/demo/corpus`
- **Budget**: 1,024 tokens
- **Model**: `qwen2.5:14b-instruct`
- Loaded **79 chunks** from 7 files
- Total corpus tokens: **4,857**

- Ollama ✓  (`qwen2.5:14b-instruct` available)

# Corpus Size Sweep

- Sweeping **N=1..7** files × **1** query

### N=1 — 1 file


**Query:** "How do I deploy to production?"

**FULL CONTEXT** — 541 tokens, 10 chunks  [██████████████████████████████]

- `CLAUDE.md` — 10 chunks, 541 tokens

**CONTEXTSEARCH** — 177 tokens, 2 chunks  [█████████░░░░░░░░░░░░░░░░░░░░░]  (67.3% saved)

- `CLAUDE.md` —   86 tok — "## Environment Variables  All configuration lives in `backend/.env` (never committed). See `app/config.py` for the full schema. Required: - `DATABASE_URL` — PostgreSQL connection string - `SECRET_KEY` — 64-byte hex string for JWT signing - `REDIS_URL` — for caching and Celery broker - `ENVIRONMENT` — `development` | `staging` | `production`"
- `CLAUDE.md` —   91 tok — "## Code Style  - Follow PEP 8. Line length 100. - Use `ruff` for linting (`ruff check .`) and `black` for formatting (`black .`). - Imports: stdlib → third-party → local, separated by blank lines. - Prefer `pathlib.Path` over `os.path`. - Use `logging.getLogger(__name__)` per module; never use `print` in production code."


### FULL CONTEXT → LLM  (10 chunks, 588 tokens sent)

Calling `qwen2.5:14b-instruct`…

**Time:** 12.0s

To deploy ProjectX to production, use Docker as specified in the `docker-compose.yml` file. First, ensure all environment variables are correctly set in your production `.env` file without committing them. Then, build and start the containers with:

```bash
docker-compose up --build -d
```

This command will handle setting up the PostgreSQL database, starting the FastAPI application, and any other services defined in `docker-compose.yml`. Make sure to review the environment variables required for production before deploying.

### CONTEXTSEARCH → LLM  (2 chunks, 219 tokens sent)

Calling `qwen2.5:14b-instruct`…

**Time:** 8.0s

To deploy to production, ensure your environment variables are correctly set in the `.env` file for the `production` environment. Use `ruff check .` and `black .` to lint and format your code according to PEP 8 standards. After testing locally or on staging, commit and push your changes to the repository, then trigger a production deployment through your CI/CD pipeline or manually update the production server with the latest codebase.

### COMPARISON SUMMARY

- **Chunks sent**: 10 → 2  (8 dropped)
- **Token reduction**: 588 → 219  (62.8% fewer)
- **Latency**: 12.0s → 8.0s  (1.5× faster, 32.7% less)

### N=2 — 2 files


**Query:** "How do I deploy to production?"

**FULL CONTEXT** — 1,237 tokens, 22 chunks  [██████████████████████████████]

- `CLAUDE.md` — 10 chunks, 541 tokens
- `api.md` — 12 chunks, 696 tokens

**CONTEXTSEARCH** — 209 tokens, 3 chunks  [█████░░░░░░░░░░░░░░░░░░░░░░░░░]  (83.1% saved)

- `api.md` —   32 tok — "## Base URL  All endpoints are relative to `/api/v1`. The full base URL in production is `https://api.projectx.io/api/v1`."
- `CLAUDE.md` —   86 tok — "## Environment Variables  All configuration lives in `backend/.env` (never committed). See `app/config.py` for the full schema. Required: - `DATABASE_URL` — PostgreSQL connection string - `SECRET_KEY` — 64-byte hex string for JWT signing - `REDIS_URL` — for caching and Celery broker - `ENVIRONMENT` — `development` | `staging` | `production`"
- `CLAUDE.md` —   91 tok — "## Code Style  - Follow PEP 8. Line length 100. - Use `ruff` for linting (`ruff check .`) and `black` for formatting (`black .`). - Imports: stdlib → third-party → local, separated by blank lines. - Prefer `pathlib.Path` over `os.path`. - Use `logging.getLogger(__name__)` per module; never use `print` in production code."


### FULL CONTEXT → LLM  (22 chunks, 1,289 tokens sent)

Calling `qwen2.5:14b-instruct`…

**Time:** 16.3s

To deploy ProjectX to production, use Docker and the provided `docker-compose.yml` file. First, ensure your environment variables are correctly set in `.env`. Then, build and start the containers with:

```bash
docker-compose up --build
```

This command sets up the PostgreSQL database, FastAPI backend, and any other services defined in the `docker-compose.yml`, ready for production use. Ensure you have configured proper secrets management and environment variables securely outside of version control.

### CONTEXTSEARCH → LLM  (3 chunks, 252 tokens sent)

Calling `qwen2.5:14b-instruct`…

**Time:** 6.9s

To deploy to production, ensure your environment variables are correctly set in the `.env` file for the production environment. After setting up the necessary configurations, commit and push your changes to the production branch. Use CI/CD pipelines or manually run deployment scripts that handle database migrations, cache clearing, and restarting the application server. Ensure all tests pass before proceeding with the final deployment steps.

### COMPARISON SUMMARY

- **Chunks sent**: 22 → 3  (19 dropped)
- **Token reduction**: 1,289 → 252  (80.4% fewer)
- **Latency**: 16.3s → 6.9s  (2.3× faster, 57.3% less)

### N=3 — 3 files


**Query:** "How do I deploy to production?"

**FULL CONTEXT** — 1,811 tokens, 31 chunks  [██████████████████████████████]

- `CLAUDE.md` — 10 chunks, 541 tokens
- `api.md` — 12 chunks, 696 tokens
- `auth.md` — 9 chunks, 574 tokens

**CONTEXTSEARCH** — 272 tokens, 4 chunks  [████░░░░░░░░░░░░░░░░░░░░░░░░░░]  (85.0% saved)

- `api.md` —   32 tok — "## Base URL  All endpoints are relative to `/api/v1`. The full base URL in production is `https://api.projectx.io/api/v1`."
- `auth.md` —   63 tok — "## Common Mistakes  - Do not set `ACCESS_TOKEN_EXPIRE_MINUTES` too high in production — 15–30 minutes is standard. - Never log or return the raw JWT in error responses. - Refresh tokens must be invalidated on logout — call `auth_service.revoke_refresh_token(token_id)`."
- `CLAUDE.md` —   86 tok — "## Environment Variables  All configuration lives in `backend/.env` (never committed). See `app/config.py` for the full schema. Required: - `DATABASE_URL` — PostgreSQL connection string - `SECRET_KEY` — 64-byte hex string for JWT signing - `REDIS_URL` — for caching and Celery broker - `ENVIRONMENT` — `development` | `staging` | `production`"
- `CLAUDE.md` —   91 tok — "## Code Style  - Follow PEP 8. Line length 100. - Use `ruff` for linting (`ruff check .`) and `black` for formatting (`black .`). - Imports: stdlib → third-party → local, separated by blank lines. - Prefer `pathlib.Path` over `os.path`. - Use `logging.getLogger(__name__)` per module; never use `print` in production code."


### FULL CONTEXT → LLM  (31 chunks, 1,869 tokens sent)

Calling `qwen2.5:14b-instruct`…

**Time:** 18.8s

To deploy ProjectX to production, use Docker and the provided `docker-compose.yml` file. Ensure your environment variables are correctly set in a `.env` file for the production environment. Then, run:

```bash
docker-compose up --build -d
```

This command builds the necessary containers and starts them in detached mode. Make sure to configure any additional infrastructure like load balancers, SSL termination, or monitoring as needed for your deployment environment.

### CONTEXTSEARCH → LLM  (4 chunks, 316 tokens sent)

Calling `qwen2.5:14b-instruct`…

**Time:** 8.2s

To deploy to production, ensure your environment variables are correctly set in the `.env` file for the production environment. Use the specified schema in `app/config.py`. After setting up the configuration, apply any necessary code changes and commit them. Then, push the updates to the production branch and trigger a deployment through your CI/CD pipeline or manually run the deployment scripts that handle database migrations, cache clearing, and restarting services if needed. Ensure thorough testing on staging before deploying to production.

### COMPARISON SUMMARY

- **Chunks sent**: 31 → 4  (27 dropped)
- **Token reduction**: 1,869 → 316  (83.1% fewer)
- **Latency**: 18.8s → 8.2s  (2.3× faster, 56.4% less)

### N=4 — 4 files


**Query:** "How do I deploy to production?"

**FULL CONTEXT** — 2,591 tokens, 43 chunks  [██████████████████████████████]

- `CLAUDE.md` — 10 chunks, 541 tokens
- `api.md` — 12 chunks, 696 tokens
- `auth.md` — 9 chunks, 574 tokens
- `database.md` — 12 chunks, 780 tokens

**CONTEXTSEARCH** — 337 tokens, 5 chunks  [███░░░░░░░░░░░░░░░░░░░░░░░░░░░]  (87.0% saved)

- `api.md` —   32 tok — "## Base URL  All endpoints are relative to `/api/v1`. The full base URL in production is `https://api.projectx.io/api/v1`."
- `auth.md` —   63 tok — "## Common Mistakes  - Do not set `ACCESS_TOKEN_EXPIRE_MINUTES` too high in production — 15–30 minutes is standard. - Never log or return the raw JWT in error responses. - Refresh tokens must be invalidated on logout — call `auth_service.revoke_refresh_token(token_id)`."
- `database.md` —   65 tok — "## Connection Pooling Notes  - `pool_pre_ping=True` detects stale connections automatically. - In production, set `pool_size` based on `(CPU cores × 2) + effective_spindle_count`. - Do not set `pool_size` above what PostgreSQL's `max_connections` can handle across all app instances."
- `CLAUDE.md` —   86 tok — "## Environment Variables  All configuration lives in `backend/.env` (never committed). See `app/config.py` for the full schema. Required: - `DATABASE_URL` — PostgreSQL connection string - `SECRET_KEY` — 64-byte hex string for JWT signing - `REDIS_URL` — for caching and Celery broker - `ENVIRONMENT` — `development` | `staging` | `production`"
- `CLAUDE.md` —   91 tok — "## Code Style  - Follow PEP 8. Line length 100. - Use `ruff` for linting (`ruff check .`) and `black` for formatting (`black .`). - Imports: stdlib → third-party → local, separated by blank lines. - Prefer `pathlib.Path` over `os.path`. - Use `logging.getLogger(__name__)` per module; never use `print` in production code."


### FULL CONTEXT → LLM  (43 chunks, 2,657 tokens sent)

Calling `qwen2.5:14b-instruct`…

**Time:** 29.5s

To deploy ProjectX to production, follow these steps:

1. Ensure your environment variables are correctly configured in `.env`, including `DATABASE_URL`, `SECRET_KEY`, and other required settings.
2. Build Docker images for the backend and frontend if using Docker (refer to `docker-compose.yml`).
3. Apply any pending database migrations with Alembic: `alembic upgrade head`.
4. Run pre-deployment checks and tests to ensure everything works as expected in a staging environment similar to production.
5. Deploy your application using your preferred method, such as Kubernetes, Docker Swarm, or directly via Docker Compose if the setup is simple enough.

Always review logs and monitor for any issues post-deployment.

### CONTEXTSEARCH → LLM  (5 chunks, 382 tokens sent)

Calling `qwen2.5:14b-instruct`…

**Time:** 9.2s

To deploy to production, ensure your environment variables are correctly set in the `.env` file for the production environment. Use the provided base URL `https://api.projectx.io/api/v1` and configure connection pooling settings according to PostgreSQL's limits. Apply proper code styling guidelines and use tools like `ruff` for linting and `black` for formatting before deployment. Ensure JWT tokens are securely managed, and refresh tokens are invalidated on logout by calling the appropriate service method.

### COMPARISON SUMMARY

- **Chunks sent**: 43 → 5  (38 dropped)
- **Token reduction**: 2,657 → 382  (85.6% fewer)
- **Latency**: 29.5s → 9.2s  (3.2× faster, 68.9% less)

### N=5 — 5 files


**Query:** "How do I deploy to production?"

**FULL CONTEXT** — 3,399 tokens, 54 chunks  [██████████████████████████████]

- `CLAUDE.md` — 10 chunks, 541 tokens
- `api.md` — 12 chunks, 696 tokens
- `auth.md` — 9 chunks, 574 tokens
- `database.md` — 12 chunks, 780 tokens
- `deployment.md` — 11 chunks, 808 tokens

**CONTEXTSEARCH** — 730 tokens, 9 chunks  [██████░░░░░░░░░░░░░░░░░░░░░░░░]  (78.5% saved)

- `api.md` —   32 tok — "## Base URL  All endpoints are relative to `/api/v1`. The full base URL in production is `https://api.projectx.io/api/v1`."
- `deployment.md` —   52 tok — "## Environments  | Environment | Branch | URL | |-------------|--------|-----| | Development | any | localhost:8000 | | Staging | `main` | staging.projectx.internal | | Production | tagged release | api.projectx.io |"
- `auth.md` —   63 tok — "## Common Mistakes  - Do not set `ACCESS_TOKEN_EXPIRE_MINUTES` too high in production — 15–30 minutes is standard. - Never log or return the raw JWT in error responses. - Refresh tokens must be invalidated on logout — call `auth_service.revoke_refresh_token(token_id)`."
- `database.md` —   65 tok — "## Connection Pooling Notes  - `pool_pre_ping=True` detects stale connections automatically. - In production, set `pool_size` based on `(CPU cores × 2) + effective_spindle_count`. - Do not set `pool_size` above what PostgreSQL's `max_connections` can handle across all app instances."
- `deployment.md` —   74 tok — "## Running Migrations in Production  Migrations run as a Kubernetes Job before the new deployment rolls out:  ```bash kubectl apply -f infra/k8s/migrate-job.yaml kubectl wait --for=condition=complete job/projectx-migrate --timeout=120s ```  Never run `alembic upgrade head` manually against production without a backup."
- `CLAUDE.md` —   86 tok — "## Environment Variables  All configuration lives in `backend/.env` (never committed). See `app/config.py` for the full schema. Required: - `DATABASE_URL` — PostgreSQL connection string - `SECRET_KEY` — 64-byte hex string for JWT signing - `REDIS_URL` — for caching and Celery broker - `ENVIRONMENT` — `development` | `staging` | `production`"
- `CLAUDE.md` —   91 tok — "## Code Style  - Follow PEP 8. Line length 100. - Use `ruff` for linting (`ruff check .`) and `black` for formatting (`black .`). - Imports: stdlib → third-party → local, separated by blank lines. - Prefer `pathlib.Path` over `os.path`. - Use `logging.getLogger(__name__)` per module; never use `print` in production code."
- `deployment.md` —  114 tok — "## CI/CD Pipeline  The pipeline runs on GitHub Actions (`.github/workflows/`):  1. **lint** — `ruff check .` and `black --check .` 2. **test** — `pytest` with a real PostgreSQL and Redis via service containers 3. **build** — Docker image built and pushed to GHCR on merge to `main` 4. **deploy-staging** — Helm upgrade to staging cluster (automatic on `main`) 5. **deploy-production** — Helm upgrade to production cluster (manual approval required)"
- `deployment.md` —  153 tok — "## Friday Deploy Protocol  Deploying to production on a **Friday is strictly forbidden** unless the on-call engineer has completed the Friday Deploy Ceremony:  1. Post the emoji sequence 🦆🔥🦆 in `#deployments` on Slack. 2. Receive a 👍 from at least two members of the **Friday Deploy Council**    (currently: @dana, @priya, and @old-reliable-bob). 3. Set your Slack status to "🎲 yolo deploy" for the duration of the rollout.  Failure to follow this protocol voids your incident post-mortem immunity. The council was established after the Thanksgiving 2022 incident. We do not speak of the Thanksgiving 2022 incident."


### FULL CONTEXT → LLM  (54 chunks, 3,469 tokens sent)

Calling `qwen2.5:14b-instruct`…

**Time:** 40.5s

Deploying to production involves several steps and requires adherence to specific protocols. Here’s how you can proceed:

1. Ensure that your code is merged into the `main` branch.
2. The CI/CD pipeline will automatically trigger a staging deployment once changes are pushed to `main`.
3. For production, you need explicit approval before proceeding with the deploy-staging workflow.
4. Follow the Friday Deploy Protocol if deploying on a Friday:
   - Post 🦆🔥🦆 in Slack's #deployments channel.
   - Receive 👍 from at least two members of the Friday Deploy Council (@dana, @priya, and @old-reliable-bob).
   - Set your Slack status to "🎲 yolo deploy".
5. Once approved, manually trigger the `deploy-production` workflow via GitHub Actions or use Helm commands directly if necessary.
6. Monitor the deployment process closely for any issues.

Always ensure thorough testing in staging before moving to production and follow all established protocols to avoid disruptions.

### CONTEXTSEARCH → LLM  (9 chunks, 779 tokens sent)

Calling `qwen2.5:14b-instruct`…

**Time:** 10.0s

To deploy to production, you must first ensure it is not a Friday unless you have completed the Friday Deploy Ceremony as outlined in the protocol. Once cleared, initiate the deployment manually via GitHub Actions by triggering the `deploy-production` workflow, which will perform a Helm upgrade to the production cluster. Ensure you follow all environmental and security best practices before proceeding.

### COMPARISON SUMMARY

- **Chunks sent**: 54 → 9  (45 dropped)
- **Token reduction**: 3,469 → 779  (77.5% fewer)
- **Latency**: 40.5s → 10.0s  (4.1× faster, 75.3% less)

### N=6 — 6 files


**Query:** "How do I deploy to production?"

**FULL CONTEXT** — 4,070 tokens, 64 chunks  [██████████████████████████████]

- `CLAUDE.md` — 10 chunks, 541 tokens
- `api.md` — 12 chunks, 696 tokens
- `auth.md` — 9 chunks, 574 tokens
- `database.md` — 12 chunks, 780 tokens
- `deployment.md` — 11 chunks, 808 tokens
- `git.md` — 10 chunks, 671 tokens

**CONTEXTSEARCH** — 905 tokens, 11 chunks  [██████░░░░░░░░░░░░░░░░░░░░░░░░]  (77.8% saved)

- `api.md` —   32 tok — "## Base URL  All endpoints are relative to `/api/v1`. The full base URL in production is `https://api.projectx.io/api/v1`."
- `deployment.md` —   52 tok — "## Environments  | Environment | Branch | URL | |-------------|--------|-----| | Development | any | localhost:8000 | | Staging | `main` | staging.projectx.internal | | Production | tagged release | api.projectx.io |"
- `auth.md` —   63 tok — "## Common Mistakes  - Do not set `ACCESS_TOKEN_EXPIRE_MINUTES` too high in production — 15–30 minutes is standard. - Never log or return the raw JWT in error responses. - Refresh tokens must be invalidated on logout — call `auth_service.revoke_refresh_token(token_id)`."
- `database.md` —   65 tok — "## Connection Pooling Notes  - `pool_pre_ping=True` detects stale connections automatically. - In production, set `pool_size` based on `(CPU cores × 2) + effective_spindle_count`. - Do not set `pool_size` above what PostgreSQL's `max_connections` can handle across all app instances."
- `deployment.md` —   74 tok — "## Running Migrations in Production  Migrations run as a Kubernetes Job before the new deployment rolls out:  ```bash kubectl apply -f infra/k8s/migrate-job.yaml kubectl wait --for=condition=complete job/projectx-migrate --timeout=120s ```  Never run `alembic upgrade head` manually against production without a backup."
- `git.md` —   83 tok — "## Branch Strategy  We use a simplified trunk-based workflow:  - `main` — always deployable, protected. All merges via PR. - `feat/<ticket>-short-description` — feature branches, short-lived (< 1 week ideally) - `fix/<ticket>-short-description` — bug fix branches - `hotfix/<ticket>-short-description` — urgent production fixes, branch from the release tag"
- `CLAUDE.md` —   86 tok — "## Environment Variables  All configuration lives in `backend/.env` (never committed). See `app/config.py` for the full schema. Required: - `DATABASE_URL` — PostgreSQL connection string - `SECRET_KEY` — 64-byte hex string for JWT signing - `REDIS_URL` — for caching and Celery broker - `ENVIRONMENT` — `development` | `staging` | `production`"
- `CLAUDE.md` —   91 tok — "## Code Style  - Follow PEP 8. Line length 100. - Use `ruff` for linting (`ruff check .`) and `black` for formatting (`black .`). - Imports: stdlib → third-party → local, separated by blank lines. - Prefer `pathlib.Path` over `os.path`. - Use `logging.getLogger(__name__)` per module; never use `print` in production code."
- `git.md` —   92 tok — "## Release Tags  Releases are tagged `v<major>.<minor>.<patch>` on `main`. The CI pipeline deploys to production on a new tag. Increment according to semver: patch for bug fixes, minor for new features, major for breaking API changes.  ```bash git tag -a v1.4.2 -m "Release v1.4.2" git push origin v1.4.2 ```"
- `deployment.md` —  114 tok — "## CI/CD Pipeline  The pipeline runs on GitHub Actions (`.github/workflows/`):  1. **lint** — `ruff check .` and `black --check .` 2. **test** — `pytest` with a real PostgreSQL and Redis via service containers 3. **build** — Docker image built and pushed to GHCR on merge to `main` 4. **deploy-staging** — Helm upgrade to staging cluster (automatic on `main`) 5. **deploy-production** — Helm upgrade to production cluster (manual approval required)"
- `deployment.md` —  153 tok — "## Friday Deploy Protocol  Deploying to production on a **Friday is strictly forbidden** unless the on-call engineer has completed the Friday Deploy Ceremony:  1. Post the emoji sequence 🦆🔥🦆 in `#deployments` on Slack. 2. Receive a 👍 from at least two members of the **Friday Deploy Council**    (currently: @dana, @priya, and @old-reliable-bob). 3. Set your Slack status to "🎲 yolo deploy" for the duration of the rollout.  Failure to follow this protocol voids your incident post-mortem immunity. The council was established after the Thanksgiving 2022 incident. We do not speak of the Thanksgiving 2022 incident."


### FULL CONTEXT → LLM  (64 chunks, 4,147 tokens sent)

Calling `qwen2.5:14b-instruct`…

**Time:** 46.3s

Deploying to production involves several steps and requires adherence to specific protocols:

1. Ensure you have completed the Friday Deploy Ceremony if deploying on a Friday, which includes posting 🦆🔥🦆 in Slack, receiving approval from the Friday Deploy Council, and setting your status to "🎲 yolo deploy".
2. The CI/CD pipeline automatically deploys to staging when changes are merged into `main`. For production deployment, you need to manually trigger the `deploy-production` workflow on GitHub Actions.
3. Before deploying, ensure all tests pass in the staging environment and that there are no pending issues or bugs reported by users.
4. Once ready, initiate a new release tag (`v<major>.<minor>.<patch>`) on `main`. The CI pipeline will automatically deploy to production upon tagging.

Always follow these steps carefully to avoid disruptions and ensure smooth deployment processes.

### CONTEXTSEARCH → LLM  (11 chunks, 958 tokens sent)

Calling `qwen2.5:14b-instruct`…

**Time:** 14.2s

To deploy to production, you must first ensure that the release is tagged with a version number following semantic versioning (e.g., `v1.4.2`). After tagging, push the tag to the remote repository. The CI/CD pipeline will automatically deploy to staging but requires manual approval for production deployment. Before deploying on a Friday, follow the Friday Deploy Protocol by posting 🦆🔥鹍 in the #deployments Slack channel and receiving 👍 from at least two members of the Friday Deploy Council.

### COMPARISON SUMMARY

- **Chunks sent**: 64 → 11  (53 dropped)
- **Token reduction**: 4,147 → 958  (76.9% fewer)
- **Latency**: 46.3s → 14.2s  (3.3× faster, 69.4% less)

### N=7 — 7 files


**Query:** "How do I deploy to production?"

**FULL CONTEXT** — 4,857 tokens, 79 chunks  [██████████████████████████████]

- `CLAUDE.md` — 10 chunks, 541 tokens
- `api.md` — 12 chunks, 696 tokens
- `auth.md` — 9 chunks, 574 tokens
- `database.md` — 12 chunks, 780 tokens
- `deployment.md` — 11 chunks, 808 tokens
- `git.md` — 10 chunks, 671 tokens
- `testing.md` — 15 chunks, 787 tokens

**CONTEXTSEARCH** — 905 tokens, 11 chunks  [█████░░░░░░░░░░░░░░░░░░░░░░░░░]  (81.4% saved)

- `api.md` —   32 tok — "## Base URL  All endpoints are relative to `/api/v1`. The full base URL in production is `https://api.projectx.io/api/v1`."
- `deployment.md` —   52 tok — "## Environments  | Environment | Branch | URL | |-------------|--------|-----| | Development | any | localhost:8000 | | Staging | `main` | staging.projectx.internal | | Production | tagged release | api.projectx.io |"
- `auth.md` —   63 tok — "## Common Mistakes  - Do not set `ACCESS_TOKEN_EXPIRE_MINUTES` too high in production — 15–30 minutes is standard. - Never log or return the raw JWT in error responses. - Refresh tokens must be invalidated on logout — call `auth_service.revoke_refresh_token(token_id)`."
- `database.md` —   65 tok — "## Connection Pooling Notes  - `pool_pre_ping=True` detects stale connections automatically. - In production, set `pool_size` based on `(CPU cores × 2) + effective_spindle_count`. - Do not set `pool_size` above what PostgreSQL's `max_connections` can handle across all app instances."
- `deployment.md` —   74 tok — "## Running Migrations in Production  Migrations run as a Kubernetes Job before the new deployment rolls out:  ```bash kubectl apply -f infra/k8s/migrate-job.yaml kubectl wait --for=condition=complete job/projectx-migrate --timeout=120s ```  Never run `alembic upgrade head` manually against production without a backup."
- `git.md` —   83 tok — "## Branch Strategy  We use a simplified trunk-based workflow:  - `main` — always deployable, protected. All merges via PR. - `feat/<ticket>-short-description` — feature branches, short-lived (< 1 week ideally) - `fix/<ticket>-short-description` — bug fix branches - `hotfix/<ticket>-short-description` — urgent production fixes, branch from the release tag"
- `CLAUDE.md` —   86 tok — "## Environment Variables  All configuration lives in `backend/.env` (never committed). See `app/config.py` for the full schema. Required: - `DATABASE_URL` — PostgreSQL connection string - `SECRET_KEY` — 64-byte hex string for JWT signing - `REDIS_URL` — for caching and Celery broker - `ENVIRONMENT` — `development` | `staging` | `production`"
- `CLAUDE.md` —   91 tok — "## Code Style  - Follow PEP 8. Line length 100. - Use `ruff` for linting (`ruff check .`) and `black` for formatting (`black .`). - Imports: stdlib → third-party → local, separated by blank lines. - Prefer `pathlib.Path` over `os.path`. - Use `logging.getLogger(__name__)` per module; never use `print` in production code."
- `git.md` —   92 tok — "## Release Tags  Releases are tagged `v<major>.<minor>.<patch>` on `main`. The CI pipeline deploys to production on a new tag. Increment according to semver: patch for bug fixes, minor for new features, major for breaking API changes.  ```bash git tag -a v1.4.2 -m "Release v1.4.2" git push origin v1.4.2 ```"
- `deployment.md` —  114 tok — "## CI/CD Pipeline  The pipeline runs on GitHub Actions (`.github/workflows/`):  1. **lint** — `ruff check .` and `black --check .` 2. **test** — `pytest` with a real PostgreSQL and Redis via service containers 3. **build** — Docker image built and pushed to GHCR on merge to `main` 4. **deploy-staging** — Helm upgrade to staging cluster (automatic on `main`) 5. **deploy-production** — Helm upgrade to production cluster (manual approval required)"
- `deployment.md` —  153 tok — "## Friday Deploy Protocol  Deploying to production on a **Friday is strictly forbidden** unless the on-call engineer has completed the Friday Deploy Ceremony:  1. Post the emoji sequence 🦆🔥🦆 in `#deployments` on Slack. 2. Receive a 👍 from at least two members of the **Friday Deploy Council**    (currently: @dana, @priya, and @old-reliable-bob). 3. Set your Slack status to "🎲 yolo deploy" for the duration of the rollout.  Failure to follow this protocol voids your incident post-mortem immunity. The council was established after the Thanksgiving 2022 incident. We do not speak of the Thanksgiving 2022 incident."


### FULL CONTEXT → LLM  (79 chunks, 4,947 tokens sent)

Calling `qwen2.5:14b-instruct`…

**Time:** 50.8s

Deploying to production on ProjectX involves a series of steps and precautions:

1. Ensure you have completed the Friday Deploy Ceremony if deploying on a Friday, which includes posting 🦆🔥🦆 in Slack and receiving approval from the Friday Deploy Council.
2. Use GitHub Actions for automated deployment processes:
   - The pipeline automatically deploys to staging when changes are merged into `main`.
   - For production, manual approval is required before proceeding with the deploy-staging workflow.
3. Apply Kubernetes Jobs to run migrations before rolling out a new app version using commands like `kubectl apply` and `kubectl wait`.
4. Roll back if necessary by reverting to an earlier Helm revision or rolling back migrations first.

Always follow the Git Workflow guidelines, including proper branch management and PR review processes, ensuring all changes are thoroughly tested in staging before production deployment.

### CONTEXTSEARCH → LLM  (11 chunks, 958 tokens sent)

Calling `qwen2.5:14b-instruct`…

**Time:** 12.6s

To deploy to production, you must first ensure that a new release tag is created on the `main` branch following semantic versioning (e.g., `git tag -a v1.4.3 -m "Release v1.4.3"` and push it). Then, manually trigger the deployment in GitHub Actions by approving the production deployment step after the pipeline automatically deploys to staging. Ensure you follow the Friday Deploy Protocol if deploying on a Friday.

### COMPARISON SUMMARY

- **Chunks sent**: 79 → 11  (68 dropped)
- **Token reduction**: 4,947 → 958  (80.6% fewer)
- **Latency**: 50.8s → 12.6s  (4.0× faster, 75.1% less)

---


### Sweep Summary

- **7** corpus sizes × **1** query = **7** benchmarks

---


