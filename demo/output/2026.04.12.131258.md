<!-- Generated: 2026.04.12.131258 -->


# ContextSearch — Real-World Demo

- **Corpus**: `/Users/coltonspurgin/Developer/contextSearch/demo/corpus`
- **Budget**: 1,024 tokens
- **Model**: `qwen2.5:14b-instruct`
- Loaded **79 chunks** from 7 files
- Total corpus tokens: **4,857**

- Ollama ✓  (`qwen2.5:14b-instruct` available)

# Corpus Size Sweep

- Sweeping **N=1..7** files × **1** query

### N=1 — 1 file


**Query:** "How do I deploy to production?"

**FULL CONTEXT** — 541 tokens, 10 chunks  [██████████████████████████████]

- `CLAUDE.md` — 10 chunks, 541 tokens

**CONTEXTSEARCH** — 177 tokens, 2 chunks  [█████████░░░░░░░░░░░░░░░░░░░░░]  (67.3% saved)

- `CLAUDE.md` —   86 tok — "## Environment Variables  All configuration lives in `backend/.env` (never committed). See `app/config.py` for the full schema. Required: - `DATABASE_URL` — PostgreSQL connection string - `SECRET_KEY` — 64-byte hex string for JWT signing - `REDIS_URL` — for caching and Celery broker - `ENVIRONMENT` — `development` | `staging` | `production`"
- `CLAUDE.md` —   91 tok — "## Code Style  - Follow PEP 8. Line length 100. - Use `ruff` for linting (`ruff check .`) and `black` for formatting (`black .`). - Imports: stdlib → third-party → local, separated by blank lines. - Prefer `pathlib.Path` over `os.path`. - Use `logging.getLogger(__name__)` per module; never use `print` in production code."


### FULL CONTEXT → LLM  (10 chunks, 588 tokens sent)

Calling `qwen2.5:14b-instruct`…

**Time:** 21.4s

To deploy ProjectX to production, use Docker Compose as it sets up the necessary environment including PostgreSQL, Redis, and any other services required by your application. Ensure all configuration variables are correctly set in `backend/.env`. Then run:

```bash
docker-compose up --build
```

This command builds the Docker images if needed and starts the containers configured for production. Make sure to review the `.env` file and adjust settings according to your production environment requirements before deploying.

### CONTEXTSEARCH → LLM  (2 chunks, 219 tokens sent)

Calling `qwen2.5:14b-instruct`…

**Time:** 6.7s

To deploy to production, ensure your environment variables are correctly set up in the `.env` file for the production environment. Use the specified tools (`ruff`, `black`) to lint and format your code according to PEP 8 guidelines. After testing thoroughly in a staging environment, commit and push your changes to the production branch and trigger the deployment process as defined by your team's workflow (e.g., via CI/CD pipeline).

### COMPARISON SUMMARY

- **Chunks sent**: 10 → 2  (8 dropped)
- **Token reduction**: 588 → 219  (62.8% fewer)
- **Latency**: 21.4s → 6.7s  (3.2× faster, 68.9% less)

### N=2 — 2 files


**Query:** "How do I deploy to production?"

**FULL CONTEXT** — 1,237 tokens, 22 chunks  [██████████████████████████████]

- `CLAUDE.md` — 10 chunks, 541 tokens
- `api.md` — 12 chunks, 696 tokens

**CONTEXTSEARCH** — 209 tokens, 3 chunks  [█████░░░░░░░░░░░░░░░░░░░░░░░░░]  (83.1% saved)

- `api.md` —   32 tok — "## Base URL  All endpoints are relative to `/api/v1`. The full base URL in production is `https://api.projectx.io/api/v1`."
- `CLAUDE.md` —   86 tok — "## Environment Variables  All configuration lives in `backend/.env` (never committed). See `app/config.py` for the full schema. Required: - `DATABASE_URL` — PostgreSQL connection string - `SECRET_KEY` — 64-byte hex string for JWT signing - `REDIS_URL` — for caching and Celery broker - `ENVIRONMENT` — `development` | `staging` | `production`"
- `CLAUDE.md` —   91 tok — "## Code Style  - Follow PEP 8. Line length 100. - Use `ruff` for linting (`ruff check .`) and `black` for formatting (`black .`). - Imports: stdlib → third-party → local, separated by blank lines. - Prefer `pathlib.Path` over `os.path`. - Use `logging.getLogger(__name__)` per module; never use `print` in production code."


### FULL CONTEXT → LLM  (22 chunks, 1,289 tokens sent)

Calling `qwen2.5:14b-instruct`…

**Time:** 14.7s

To deploy ProjectX to production, use Docker and the provided `docker-compose.yml` file. First, ensure your environment variables are correctly set in `.env`. Then, build and start the containers with:

```bash
docker-compose up --build
```

This command will handle setting up the PostgreSQL database, starting FastAPI, and any other services defined in the Docker Compose configuration. Make sure to review and adjust the `docker-compose.yml` file if custom configurations are needed for production environments.

### CONTEXTSEARCH → LLM  (3 chunks, 252 tokens sent)

Calling `qwen2.5:14b-instruct`…

**Time:** 6.2s

To deploy to production, ensure your environment variables are correctly set in the `.env` file for the `production` environment. After setting up the configuration, commit and push your changes to the production branch. Use the CI/CD pipeline configured for production to automatically handle the deployment process from this point. Ensure you have tested thoroughly in staging before deploying to avoid issues in production.

### COMPARISON SUMMARY

- **Chunks sent**: 22 → 3  (19 dropped)
- **Token reduction**: 1,289 → 252  (80.4% fewer)
- **Latency**: 14.7s → 6.2s  (2.4× faster, 58.0% less)

### N=3 — 3 files


**Query:** "How do I deploy to production?"

**FULL CONTEXT** — 1,811 tokens, 31 chunks  [██████████████████████████████]

- `CLAUDE.md` — 10 chunks, 541 tokens
- `api.md` — 12 chunks, 696 tokens
- `auth.md` — 9 chunks, 574 tokens

**CONTEXTSEARCH** — 272 tokens, 4 chunks  [████░░░░░░░░░░░░░░░░░░░░░░░░░░]  (85.0% saved)

- `api.md` —   32 tok — "## Base URL  All endpoints are relative to `/api/v1`. The full base URL in production is `https://api.projectx.io/api/v1`."
- `auth.md` —   63 tok — "## Common Mistakes  - Do not set `ACCESS_TOKEN_EXPIRE_MINUTES` too high in production — 15–30 minutes is standard. - Never log or return the raw JWT in error responses. - Refresh tokens must be invalidated on logout — call `auth_service.revoke_refresh_token(token_id)`."
- `CLAUDE.md` —   86 tok — "## Environment Variables  All configuration lives in `backend/.env` (never committed). See `app/config.py` for the full schema. Required: - `DATABASE_URL` — PostgreSQL connection string - `SECRET_KEY` — 64-byte hex string for JWT signing - `REDIS_URL` — for caching and Celery broker - `ENVIRONMENT` — `development` | `staging` | `production`"
- `CLAUDE.md` —   91 tok — "## Code Style  - Follow PEP 8. Line length 100. - Use `ruff` for linting (`ruff check .`) and `black` for formatting (`black .`). - Imports: stdlib → third-party → local, separated by blank lines. - Prefer `pathlib.Path` over `os.path`. - Use `logging.getLogger(__name__)` per module; never use `print` in production code."


### FULL CONTEXT → LLM  (31 chunks, 1,869 tokens sent)

Calling `qwen2.5:14b-instruct`…

**Time:** 18.3s

To deploy ProjectX to production, use Docker and docker-compose as specified in the repository. Ensure your environment variables are correctly set up in a `.env` file for the production environment before running:

```bash
docker-compose up --build
```

This command builds the necessary Docker images and starts the containers configured for production. Make sure all required environment variables like `DATABASE_URL`, `SECRET_KEY`, etc., are properly defined and secured, as they should not be committed to version control.

### CONTEXTSEARCH → LLM  (4 chunks, 316 tokens sent)

Calling `qwen2.5:14b-instruct`…

**Time:** 7.6s

To deploy to production, ensure your environment is correctly configured with the appropriate values in `backend/.env`. Use the specified tools for linting and formatting (ruff and black) before deployment. Follow the provided guidelines on line length and import order to maintain code style consistency. Additionally, adhere to security practices such as setting a reasonable `ACCESS_TOKEN_EXPIRE_MINUTES` value and properly handling JWTs to avoid exposing sensitive information.

### COMPARISON SUMMARY

- **Chunks sent**: 31 → 4  (27 dropped)
- **Token reduction**: 1,869 → 316  (83.1% fewer)
- **Latency**: 18.3s → 7.6s  (2.4× faster, 58.4% less)

### N=4 — 4 files


**Query:** "How do I deploy to production?"

**FULL CONTEXT** — 2,591 tokens, 43 chunks  [██████████████████████████████]

- `CLAUDE.md` — 10 chunks, 541 tokens
- `api.md` — 12 chunks, 696 tokens
- `auth.md` — 9 chunks, 574 tokens
- `database.md` — 12 chunks, 780 tokens

**CONTEXTSEARCH** — 337 tokens, 5 chunks  [███░░░░░░░░░░░░░░░░░░░░░░░░░░░]  (87.0% saved)

- `api.md` —   32 tok — "## Base URL  All endpoints are relative to `/api/v1`. The full base URL in production is `https://api.projectx.io/api/v1`."
- `auth.md` —   63 tok — "## Common Mistakes  - Do not set `ACCESS_TOKEN_EXPIRE_MINUTES` too high in production — 15–30 minutes is standard. - Never log or return the raw JWT in error responses. - Refresh tokens must be invalidated on logout — call `auth_service.revoke_refresh_token(token_id)`."
- `database.md` —   65 tok — "## Connection Pooling Notes  - `pool_pre_ping=True` detects stale connections automatically. - In production, set `pool_size` based on `(CPU cores × 2) + effective_spindle_count`. - Do not set `pool_size` above what PostgreSQL's `max_connections` can handle across all app instances."
- `CLAUDE.md` —   86 tok — "## Environment Variables  All configuration lives in `backend/.env` (never committed). See `app/config.py` for the full schema. Required: - `DATABASE_URL` — PostgreSQL connection string - `SECRET_KEY` — 64-byte hex string for JWT signing - `REDIS_URL` — for caching and Celery broker - `ENVIRONMENT` — `development` | `staging` | `production`"
- `CLAUDE.md` —   91 tok — "## Code Style  - Follow PEP 8. Line length 100. - Use `ruff` for linting (`ruff check .`) and `black` for formatting (`black .`). - Imports: stdlib → third-party → local, separated by blank lines. - Prefer `pathlib.Path` over `os.path`. - Use `logging.getLogger(__name__)` per module; never use `print` in production code."


### FULL CONTEXT → LLM  (43 chunks, 2,657 tokens sent)

Calling `qwen2.5:14b-instruct`…

**Time:** 28.8s

To deploy ProjectX to production, follow these steps:

1. Ensure your environment variables are correctly configured in `.env`, especially for `DATABASE_URL`, `SECRET_KEY`, and `REDIS_URL`.
2. Build Docker images and push them to a container registry.
3. Update the `docker-compose.yml` file with the correct image tags and any necessary production settings.
4. Use `docker-compose up --build` to start services in production mode.
5. Apply database migrations using Alembic: `alembic upgrade head`.
6. Verify that rate limiting, authentication, and authorization are properly configured for the production environment.

Always test changes thoroughly in a staging environment before deploying to production.

### CONTEXTSEARCH → LLM  (5 chunks, 382 tokens sent)

Calling `qwen2.5:14b-instruct`…

**Time:** 10.7s

To deploy to production, ensure your environment variables are correctly set in the `.env` file for the production environment. Review and adjust connection pooling settings as necessary based on PostgreSQL's `max_connections`. Use CI/CD pipelines if available to automate deployment processes, ensuring that all configurations match the production requirements specified in the context. Finally, run linting checks with `ruff` and format code with `black` before deploying to maintain consistency and quality.

### COMPARISON SUMMARY

- **Chunks sent**: 43 → 5  (38 dropped)
- **Token reduction**: 2,657 → 382  (85.6% fewer)
- **Latency**: 28.8s → 10.7s  (2.7× faster, 62.7% less)

### N=5 — 5 files


**Query:** "How do I deploy to production?"

**FULL CONTEXT** — 3,399 tokens, 54 chunks  [██████████████████████████████]

- `CLAUDE.md` — 10 chunks, 541 tokens
- `api.md` — 12 chunks, 696 tokens
- `auth.md` — 9 chunks, 574 tokens
- `database.md` — 12 chunks, 780 tokens
- `deployment.md` — 11 chunks, 808 tokens

**CONTEXTSEARCH** — 730 tokens, 9 chunks  [██████░░░░░░░░░░░░░░░░░░░░░░░░]  (78.5% saved)

- `api.md` —   32 tok — "## Base URL  All endpoints are relative to `/api/v1`. The full base URL in production is `https://api.projectx.io/api/v1`."
- `deployment.md` —   52 tok — "## Environments  | Environment | Branch | URL | |-------------|--------|-----| | Development | any | localhost:8000 | | Staging | `main` | staging.projectx.internal | | Production | tagged release | api.projectx.io |"
- `auth.md` —   63 tok — "## Common Mistakes  - Do not set `ACCESS_TOKEN_EXPIRE_MINUTES` too high in production — 15–30 minutes is standard. - Never log or return the raw JWT in error responses. - Refresh tokens must be invalidated on logout — call `auth_service.revoke_refresh_token(token_id)`."
- `database.md` —   65 tok — "## Connection Pooling Notes  - `pool_pre_ping=True` detects stale connections automatically. - In production, set `pool_size` based on `(CPU cores × 2) + effective_spindle_count`. - Do not set `pool_size` above what PostgreSQL's `max_connections` can handle across all app instances."
- `deployment.md` —   74 tok — "## Running Migrations in Production  Migrations run as a Kubernetes Job before the new deployment rolls out:  ```bash kubectl apply -f infra/k8s/migrate-job.yaml kubectl wait --for=condition=complete job/projectx-migrate --timeout=120s ```  Never run `alembic upgrade head` manually against production without a backup."
- `CLAUDE.md` —   86 tok — "## Environment Variables  All configuration lives in `backend/.env` (never committed). See `app/config.py` for the full schema. Required: - `DATABASE_URL` — PostgreSQL connection string - `SECRET_KEY` — 64-byte hex string for JWT signing - `REDIS_URL` — for caching and Celery broker - `ENVIRONMENT` — `development` | `staging` | `production`"
- `CLAUDE.md` —   91 tok — "## Code Style  - Follow PEP 8. Line length 100. - Use `ruff` for linting (`ruff check .`) and `black` for formatting (`black .`). - Imports: stdlib → third-party → local, separated by blank lines. - Prefer `pathlib.Path` over `os.path`. - Use `logging.getLogger(__name__)` per module; never use `print` in production code."
- `deployment.md` —  114 tok — "## CI/CD Pipeline  The pipeline runs on GitHub Actions (`.github/workflows/`):  1. **lint** — `ruff check .` and `black --check .` 2. **test** — `pytest` with a real PostgreSQL and Redis via service containers 3. **build** — Docker image built and pushed to GHCR on merge to `main` 4. **deploy-staging** — Helm upgrade to staging cluster (automatic on `main`) 5. **deploy-production** — Helm upgrade to production cluster (manual approval required)"
- `deployment.md` —  153 tok — "## Friday Deploy Protocol  Deploying to production on a **Friday is strictly forbidden** unless the on-call engineer has completed the Friday Deploy Ceremony:  1. Post the emoji sequence 🦆🔥🦆 in `#deployments` on Slack. 2. Receive a 👍 from at least two members of the **Friday Deploy Council**    (currently: @dana, @priya, and @old-reliable-bob). 3. Set your Slack status to "🎲 yolo deploy" for the duration of the rollout.  Failure to follow this protocol voids your incident post-mortem immunity. The council was established after the Thanksgiving 2022 incident. We do not speak of the Thanksgiving 2022 incident."


### FULL CONTEXT → LLM  (54 chunks, 3,469 tokens sent)

Calling `qwen2.5:14b-instruct`…

**Time:** 37.5s

Deploying to production involves several steps and requires adherence to specific protocols. Here’s how you can proceed:

1. Ensure that your code is merged into the `main` branch.
2. The CI/CD pipeline will automatically trigger a staging deployment when changes are pushed to `main`.
3. For production, manually approve the `deploy-production` step in GitHub Actions after testing thoroughly on staging.
4. Follow the Friday Deploy Protocol if deploying on a Friday:
   - Post 🦆🔥🦆 in Slack's #deployments channel.
   - Receive 👍 from at least two members of the Friday Deploy Council (@dana, @priya, and @old-reliable-bob).
   - Set your Slack status to "🎲 yolo deploy" for the duration of the rollout.

After approval, Helm will upgrade the production cluster with the latest Docker image built during the pipeline.

### CONTEXTSEARCH → LLM  (9 chunks, 779 tokens sent)

Calling `qwen2.5:14b-instruct`…

**Time:** 9.9s

To deploy to production, you must first ensure it is not a Friday unless you have completed the Friday Deploy Ceremony as outlined in the protocol. Then, initiate the deployment through GitHub Actions by triggering the `deploy-production` job manually from the repository's workflow settings. This requires manual approval due to its critical nature. Ensure all previous steps (linting, testing, building) are successful before proceeding with production deployment.

### COMPARISON SUMMARY

- **Chunks sent**: 54 → 9  (45 dropped)
- **Token reduction**: 3,469 → 779  (77.5% fewer)
- **Latency**: 37.5s → 9.9s  (3.8× faster, 73.5% less)

### N=6 — 6 files


**Query:** "How do I deploy to production?"

**FULL CONTEXT** — 4,070 tokens, 64 chunks  [██████████████████████████████]

- `CLAUDE.md` — 10 chunks, 541 tokens
- `api.md` — 12 chunks, 696 tokens
- `auth.md` — 9 chunks, 574 tokens
- `database.md` — 12 chunks, 780 tokens
- `deployment.md` — 11 chunks, 808 tokens
- `git.md` — 10 chunks, 671 tokens

**CONTEXTSEARCH** — 905 tokens, 11 chunks  [██████░░░░░░░░░░░░░░░░░░░░░░░░]  (77.8% saved)

- `api.md` —   32 tok — "## Base URL  All endpoints are relative to `/api/v1`. The full base URL in production is `https://api.projectx.io/api/v1`."
- `deployment.md` —   52 tok — "## Environments  | Environment | Branch | URL | |-------------|--------|-----| | Development | any | localhost:8000 | | Staging | `main` | staging.projectx.internal | | Production | tagged release | api.projectx.io |"
- `auth.md` —   63 tok — "## Common Mistakes  - Do not set `ACCESS_TOKEN_EXPIRE_MINUTES` too high in production — 15–30 minutes is standard. - Never log or return the raw JWT in error responses. - Refresh tokens must be invalidated on logout — call `auth_service.revoke_refresh_token(token_id)`."
- `database.md` —   65 tok — "## Connection Pooling Notes  - `pool_pre_ping=True` detects stale connections automatically. - In production, set `pool_size` based on `(CPU cores × 2) + effective_spindle_count`. - Do not set `pool_size` above what PostgreSQL's `max_connections` can handle across all app instances."
- `deployment.md` —   74 tok — "## Running Migrations in Production  Migrations run as a Kubernetes Job before the new deployment rolls out:  ```bash kubectl apply -f infra/k8s/migrate-job.yaml kubectl wait --for=condition=complete job/projectx-migrate --timeout=120s ```  Never run `alembic upgrade head` manually against production without a backup."
- `git.md` —   83 tok — "## Branch Strategy  We use a simplified trunk-based workflow:  - `main` — always deployable, protected. All merges via PR. - `feat/<ticket>-short-description` — feature branches, short-lived (< 1 week ideally) - `fix/<ticket>-short-description` — bug fix branches - `hotfix/<ticket>-short-description` — urgent production fixes, branch from the release tag"
- `CLAUDE.md` —   86 tok — "## Environment Variables  All configuration lives in `backend/.env` (never committed). See `app/config.py` for the full schema. Required: - `DATABASE_URL` — PostgreSQL connection string - `SECRET_KEY` — 64-byte hex string for JWT signing - `REDIS_URL` — for caching and Celery broker - `ENVIRONMENT` — `development` | `staging` | `production`"
- `CLAUDE.md` —   91 tok — "## Code Style  - Follow PEP 8. Line length 100. - Use `ruff` for linting (`ruff check .`) and `black` for formatting (`black .`). - Imports: stdlib → third-party → local, separated by blank lines. - Prefer `pathlib.Path` over `os.path`. - Use `logging.getLogger(__name__)` per module; never use `print` in production code."
- `git.md` —   92 tok — "## Release Tags  Releases are tagged `v<major>.<minor>.<patch>` on `main`. The CI pipeline deploys to production on a new tag. Increment according to semver: patch for bug fixes, minor for new features, major for breaking API changes.  ```bash git tag -a v1.4.2 -m "Release v1.4.2" git push origin v1.4.2 ```"
- `deployment.md` —  114 tok — "## CI/CD Pipeline  The pipeline runs on GitHub Actions (`.github/workflows/`):  1. **lint** — `ruff check .` and `black --check .` 2. **test** — `pytest` with a real PostgreSQL and Redis via service containers 3. **build** — Docker image built and pushed to GHCR on merge to `main` 4. **deploy-staging** — Helm upgrade to staging cluster (automatic on `main`) 5. **deploy-production** — Helm upgrade to production cluster (manual approval required)"
- `deployment.md` —  153 tok — "## Friday Deploy Protocol  Deploying to production on a **Friday is strictly forbidden** unless the on-call engineer has completed the Friday Deploy Ceremony:  1. Post the emoji sequence 🦆🔥🦆 in `#deployments` on Slack. 2. Receive a 👍 from at least two members of the **Friday Deploy Council**    (currently: @dana, @priya, and @old-reliable-bob). 3. Set your Slack status to "🎲 yolo deploy" for the duration of the rollout.  Failure to follow this protocol voids your incident post-mortem immunity. The council was established after the Thanksgiving 2022 incident. We do not speak of the Thanksgiving 2022 incident."


### FULL CONTEXT → LLM  (64 chunks, 4,147 tokens sent)

Calling `qwen2.5:14b-instruct`…

**Time:** 43.2s

Deploying to production on ProjectX involves a series of steps and requires adherence to specific protocols:

1. Ensure you have completed the Friday Deploy Ceremony if deploying on a Friday, which includes posting 🦆🔥🦆 in Slack, receiving 👍 from at least two members of the Friday Deploy Council, and setting your Slack status to "🎲 yolo deploy".
2. Use Helm for deployment: Apply the production Helm chart located in `infra/helm/projectx/`.
3. Execute a manual approval process as the pipeline requires explicit permission before deploying to the production cluster.
4. Roll out migrations using Kubernetes Jobs to ensure database schema updates are applied correctly before the new application version is deployed.

Always follow these steps carefully and consult with team members if unsure about any part of the deployment process, especially when handling secrets or performing rollbacks.

### CONTEXTSEARCH → LLM  (11 chunks, 958 tokens sent)

Calling `qwen2.5:14b-instruct`…

**Time:** 13.1s

To deploy to production, you must first ensure that a release tag is created on the `main` branch following semantic versioning (e.g., `git tag -a v1.4.2 -m "Release v1.4.2"` and `git push origin v1.4.2`). Then, manually trigger the production deployment in GitHub Actions by approving the release pipeline after it has automatically run through development and staging environments. Ensure you follow the Friday Deploy Protocol if deploying on a Friday to maintain system integrity and avoid incidents.

### COMPARISON SUMMARY

- **Chunks sent**: 64 → 11  (53 dropped)
- **Token reduction**: 4,147 → 958  (76.9% fewer)
- **Latency**: 43.2s → 13.1s  (3.3× faster, 69.6% less)

### N=7 — 7 files


**Query:** "How do I deploy to production?"

**FULL CONTEXT** — 4,857 tokens, 79 chunks  [██████████████████████████████]

- `CLAUDE.md` — 10 chunks, 541 tokens
- `api.md` — 12 chunks, 696 tokens
- `auth.md` — 9 chunks, 574 tokens
- `database.md` — 12 chunks, 780 tokens
- `deployment.md` — 11 chunks, 808 tokens
- `git.md` — 10 chunks, 671 tokens
- `testing.md` — 15 chunks, 787 tokens

**CONTEXTSEARCH** — 905 tokens, 11 chunks  [█████░░░░░░░░░░░░░░░░░░░░░░░░░]  (81.4% saved)

- `api.md` —   32 tok — "## Base URL  All endpoints are relative to `/api/v1`. The full base URL in production is `https://api.projectx.io/api/v1`."
- `deployment.md` —   52 tok — "## Environments  | Environment | Branch | URL | |-------------|--------|-----| | Development | any | localhost:8000 | | Staging | `main` | staging.projectx.internal | | Production | tagged release | api.projectx.io |"
- `auth.md` —   63 tok — "## Common Mistakes  - Do not set `ACCESS_TOKEN_EXPIRE_MINUTES` too high in production — 15–30 minutes is standard. - Never log or return the raw JWT in error responses. - Refresh tokens must be invalidated on logout — call `auth_service.revoke_refresh_token(token_id)`."
- `database.md` —   65 tok — "## Connection Pooling Notes  - `pool_pre_ping=True` detects stale connections automatically. - In production, set `pool_size` based on `(CPU cores × 2) + effective_spindle_count`. - Do not set `pool_size` above what PostgreSQL's `max_connections` can handle across all app instances."
- `deployment.md` —   74 tok — "## Running Migrations in Production  Migrations run as a Kubernetes Job before the new deployment rolls out:  ```bash kubectl apply -f infra/k8s/migrate-job.yaml kubectl wait --for=condition=complete job/projectx-migrate --timeout=120s ```  Never run `alembic upgrade head` manually against production without a backup."
- `git.md` —   83 tok — "## Branch Strategy  We use a simplified trunk-based workflow:  - `main` — always deployable, protected. All merges via PR. - `feat/<ticket>-short-description` — feature branches, short-lived (< 1 week ideally) - `fix/<ticket>-short-description` — bug fix branches - `hotfix/<ticket>-short-description` — urgent production fixes, branch from the release tag"
- `CLAUDE.md` —   86 tok — "## Environment Variables  All configuration lives in `backend/.env` (never committed). See `app/config.py` for the full schema. Required: - `DATABASE_URL` — PostgreSQL connection string - `SECRET_KEY` — 64-byte hex string for JWT signing - `REDIS_URL` — for caching and Celery broker - `ENVIRONMENT` — `development` | `staging` | `production`"
- `CLAUDE.md` —   91 tok — "## Code Style  - Follow PEP 8. Line length 100. - Use `ruff` for linting (`ruff check .`) and `black` for formatting (`black .`). - Imports: stdlib → third-party → local, separated by blank lines. - Prefer `pathlib.Path` over `os.path`. - Use `logging.getLogger(__name__)` per module; never use `print` in production code."
- `git.md` —   92 tok — "## Release Tags  Releases are tagged `v<major>.<minor>.<patch>` on `main`. The CI pipeline deploys to production on a new tag. Increment according to semver: patch for bug fixes, minor for new features, major for breaking API changes.  ```bash git tag -a v1.4.2 -m "Release v1.4.2" git push origin v1.4.2 ```"
- `deployment.md` —  114 tok — "## CI/CD Pipeline  The pipeline runs on GitHub Actions (`.github/workflows/`):  1. **lint** — `ruff check .` and `black --check .` 2. **test** — `pytest` with a real PostgreSQL and Redis via service containers 3. **build** — Docker image built and pushed to GHCR on merge to `main` 4. **deploy-staging** — Helm upgrade to staging cluster (automatic on `main`) 5. **deploy-production** — Helm upgrade to production cluster (manual approval required)"
- `deployment.md` —  153 tok — "## Friday Deploy Protocol  Deploying to production on a **Friday is strictly forbidden** unless the on-call engineer has completed the Friday Deploy Ceremony:  1. Post the emoji sequence 🦆🔥🦆 in `#deployments` on Slack. 2. Receive a 👍 from at least two members of the **Friday Deploy Council**    (currently: @dana, @priya, and @old-reliable-bob). 3. Set your Slack status to "🎲 yolo deploy" for the duration of the rollout.  Failure to follow this protocol voids your incident post-mortem immunity. The council was established after the Thanksgiving 2022 incident. We do not speak of the Thanksgiving 2022 incident."


### FULL CONTEXT → LLM  (79 chunks, 4,947 tokens sent)

Calling `qwen2.5:14b-instruct`…

**Time:** 54.3s

Deploying to production on ProjectX involves a series of steps and precautions:

1. Ensure you have completed the Friday Deploy Ceremony if deploying on a Friday, which includes posting 🦆🔥🦆 in Slack, receiving approval from at least two members of the Friday Deploy Council, and setting your Slack status to "🎲 yolo deploy".

2. Use Helm for deployments. The production deployment is triggered manually after review.

3. Apply migrations before rolling out new code:
   ```bash
   kubectl apply -f infra/k8s/migrate-job.yaml
   kubectl wait --for=condition=complete job/projectx-migrate --timeout=120s
   ```

4. Roll out the new deployment using Helm:
   ```bash
   helm upgrade projectx ./infra/helm/projectx/ --set image.tag=<new_tag>
   ```
   
5. Monitor the rollout and perform a rollback if necessary:
   ```bash
   helm rollback projectx <revision>
   ```

6. Always follow the Git workflow for tagging releases (`v<major>.<minor>.<patch>` on `main`) to trigger production deployment via CI/CD.

Remember, any changes involving secrets management or requiring secret key rotation should be coordinated with the team and handled

### CONTEXTSEARCH → LLM  (11 chunks, 958 tokens sent)

Calling `qwen2.5:14b-instruct`…

**Time:** 14.4s

To deploy to production, you must first ensure that a release tag is created on the `main` branch following semantic versioning (e.g., `git tag -a v1.4.3 -m "Release v1.4.3"` and push it). Then, manually trigger the deployment in GitHub Actions by approving the `deploy-production` job after it has automatically run through linting, testing, building, and staging deployment steps. Ensure you follow the Friday Deploy Protocol if deploying on a Friday to maintain incident post-mortem immunity.

### COMPARISON SUMMARY

- **Chunks sent**: 79 → 11  (68 dropped)
- **Token reduction**: 4,947 → 958  (80.6% fewer)
- **Latency**: 54.3s → 14.4s  (3.8× faster, 73.4% less)

---


### Sweep Summary

- **7** corpus sizes × **1** query = **7** benchmarks

---


