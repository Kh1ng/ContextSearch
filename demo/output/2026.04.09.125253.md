<!-- Generated: 2026.04.09.125253 -->


# ContextSearch — Real-World Demo

- **Corpus**: `/Users/coltonspurgin/Developer/contextSearch/demo/corpus`
- **Budget**: 1,024 tokens
- **Model**: `qwen2.5:14b-instruct`
- Loaded **76 chunks** from 7 files
- Total corpus tokens: **4,410**

- Ollama ✓  (`qwen2.5:14b-instruct` available)

# Query


**Query:** "What do I need to do to change our database to mongo db?"

**FULL CONTEXT** — 4,410 tokens, 76 chunks  [██████████████████████████████]

- `CLAUDE.md` — 10 chunks, 541 tokens
- `api.md` — 12 chunks, 696 tokens
- `auth.md` — 9 chunks, 574 tokens
- `database.md` — 12 chunks, 682 tokens
- `deployment.md` — 10 chunks, 655 tokens
- `git.md` — 10 chunks, 579 tokens
- `testing.md` — 13 chunks, 683 tokens

**CONTEXTSEARCH** — 968 tokens, 14 chunks  [██████░░░░░░░░░░░░░░░░░░░░░░░░]  (78.0% saved)

- `database.md` —    2 tok — "# Database"
- `deployment.md` —   45 tok — "## Health Check  `GET /health` returns `{"status": "ok"}` with a 200. It also checks DB and Redis connectivity — if either is down it returns 503. Never remove this endpoint."
- `testing.md` —   47 tok — "## What Not to Mock  Do not mock the database session or repository layer in integration tests. Mock only external services (email, Stripe, third-party APIs) using `pytest-mock` or `respx` for HTTP clients."
- `testing.md` —   59 tok — "## Stack  - **pytest** + **pytest-asyncio** for all tests - **httpx.AsyncClient** for route-level integration tests - **factory_boy** for test data factories - A real PostgreSQL instance (via Docker) — no SQLite or mocks for DB tests"
- `testing.md` —   61 tok — "## Factories  Use factories instead of hardcoding fixture data:  ```python from tests.factories import UserFactory, ItemFactory  user = await UserFactory.create(role="member", db=db) items = await ItemFactory.create_batch(5, tenant_id=user.tenant_id, db=db) ```"
- `database.md` —   65 tok — "## Connection Pooling Notes  - `pool_pre_ping=True` detects stale connections automatically. - In production, set `pool_size` based on `(CPU cores × 2) + effective_spindle_count`. - Do not set `pool_size` above what PostgreSQL's `max_connections` can handle across all app instances."
- `git.md` —   67 tok — "## Pull Requests  - PR title should match the commit message format. - Fill out the PR template (bug fix / feature / what was tested). - Request at least one reviewer — two for anything touching auth or billing. - Squash merge to keep `main` history clean. The squash commit message should be the PR title."
- `database.md` —   74 tok — "## Session Dependency  Always acquire a session via the FastAPI dependency — never construct one manually in route handlers:  ```python async def get_db() -> AsyncGenerator[AsyncSession, None]:     async with AsyncSessionLocal() as session:         try:             yield session             await session.commit()         except Exception:             await session.rollback()             raise ```"
- `auth.md` —   82 tok — "## Role-Based Access Control  Roles are stored on the `User` model: `admin`, `member`, `viewer`. Enforce with the `require_role` dependency:  ```python @router.delete("/{item_id}") async def delete_item(     item_id: UUID,     current_user: User = Depends(require_role("admin")),     db: AsyncSession = Depends(get_db), ):     ... ```"
- `git.md` —   82 tok — "# Bad — vague, no type git commit -m "fix stuff" git commit -m "WIP" ```  Keep subject lines under 72 characters. Reference the issue number in the footer:  ``` feat(items): add soft delete support  Items are now marked deleted_at rather than removed from the DB. Cascades to child records via a DB trigger.  closes #234 ```"
- `CLAUDE.md` —   86 tok — "## Environment Variables  All configuration lives in `backend/.env` (never committed). See `app/config.py` for the full schema. Required: - `DATABASE_URL` — PostgreSQL connection string - `SECRET_KEY` — 64-byte hex string for JWT signing - `REDIS_URL` — for caching and Celery broker - `ENVIRONMENT` — `development` | `staging` | `production`"
- `CLAUDE.md` —   92 tok — "## Core Principles  - **Correctness first.** Never sacrifice correctness for brevity. If a change is risky, say so. - **Minimal surface area.** Prefer editing existing files over creating new ones. - **No silent failures.** Every error path must either raise an exception or log at WARNING or above. - **Type everything.** All Python functions must have full type annotations. Avoid `Any` unless wrapping a third-party interface."
- `testing.md` —   94 tok — "## Test Database  Tests run against a separate `projectx_test` database. The `conftest.py` in `tests/` creates all tables before the session and drops them after. Each test gets a transaction that is rolled back — no cleanup needed per test.  Set `TEST_DATABASE_URL` in your environment or `.env.test`: ``` TEST_DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/projectx_test ```"
- `CLAUDE.md` —  112 tok — "## Repository Layout  ``` backend/   app/     main.py          # FastAPI app factory     config.py        # Settings via pydantic-settings     models/          # SQLAlchemy ORM models     schemas/         # Pydantic request/response schemas     routers/         # API route handlers     services/        # Business logic layer     repositories/    # Database access layer     middleware/       # Auth, logging, error handling   migrations/        # Alembic migration files   tests/   requirements.txt ```"


### FULL CONTEXT → LLM  (76 chunks, 4,506 tokens sent)

Calling `qwen2.5:14b-instruct`…

**Time:** 57.6s

To change ProjectX's database from PostgreSQL to MongoDB, you would need
to undertake a significant overhaul of the existing codebase and
architecture. Here are some key steps:  1. **Update Dependencies**:
Remove SQLAlchemy and Alembic dependencies since they are specific to
SQL databases. Add PyMongo or Motor (for asynchronous operations) as
your new MongoDB client.  2. **Model Changes**: Convert all SQLAlchemy
ORM models in `app/models/` to match the structure expected by MongoDB,
which is schema-less but typically uses a more document-oriented
approach.  3. **Repository Layer**: Rewrite repository classes in
`app/repositories/` to use PyMongo or Motor instead of SQLAlchemy
sessions for CRUD operations and queries.  4. **Authentication &
Authorization**: Ensure that JWT tokens continue to work correctly with
the new database setup, especially if roles and user data are stored
differently.  5. **Testing**: Update test fixtures and factories to
create MongoDB collections rather than SQL tables. Adjust integration
tests to use PyMongo/Motor for database interactions.  6. **Deployment &
Environment Variables**: Modify `.env` files and Docker configurations
to include MongoDB connection strings instead of PostgreSQL URLs.  7.
**Rate Limiting & Caching**: If Redis was used as a cache layer, ensure
that the new setup still provides equivalent

### CONTEXTSEARCH → LLM  (14 chunks, 1,037 tokens sent)

Calling `qwen2.5:14b-instruct`…

**Time:** 13.9s

To change the database from PostgreSQL to MongoDB, you would need to:
1. Update your project's data access layer (`repositories/`) to use a
MongoDB client instead of SQLAlchemy. 2. Migrate existing data from
PostgreSQL to MongoDB if necessary. 3. Adjust schemas and models in
`app/models` and `app/schemas` to match MongoDB's schema-less structure
or define appropriate schemas for consistency. 4. Update environment
variables to include MongoDB connection settings (`DATABASE_URL`) rather
than PostgreSQL ones.  Ensure all tests are updated to reflect these
changes, using a test MongoDB instance instead of PostgreSQL during
testing.

### COMPARISON SUMMARY

- **Chunks sent**: 76 → 14  (62 dropped)
- **Token reduction**: 4,506 → 1,037  (77.0% fewer)
- **Latency**: 57.6s → 13.9s  (4.1× faster, 75.8% less)

---


### Summary

- **Average token savings** across 1 queries: **78.0%**
- Full corpus: **4,410** tokens → avg ContextSearch: **968** tokens

---


