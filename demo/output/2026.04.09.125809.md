<!-- Generated: 2026.04.09.125809 -->


# ContextSearch — Real-World Demo

- **Corpus**: `/Users/coltonspurgin/Developer/contextSearch/demo/corpus`
- **Budget**: 1,024 tokens
- **Model**: `qwen2.5:14b-instruct`
- Loaded **79 chunks** from 7 files
- Total corpus tokens: **4,857**

- Ollama ✓  (`qwen2.5:14b-instruct` available)

# Query


**Query:** "What do I need to do to migrate and deploy my database on a friday?"

**FULL CONTEXT** — 4,857 tokens, 79 chunks  [██████████████████████████████]

- `CLAUDE.md` — 10 chunks, 541 tokens
- `api.md` — 12 chunks, 696 tokens
- `auth.md` — 9 chunks, 574 tokens
- `database.md` — 12 chunks, 780 tokens
- `deployment.md` — 11 chunks, 808 tokens
- `git.md` — 10 chunks, 671 tokens
- `testing.md` — 15 chunks, 787 tokens

**CONTEXTSEARCH** — 967 tokens, 11 chunks  [█████░░░░░░░░░░░░░░░░░░░░░░░░░]  (80.1% saved)

- `database.md` —    2 tok — "# Database"
- `testing.md` —   47 tok — "## What Not to Mock  Do not mock the database session or repository layer in integration tests. Mock only external services (email, Stripe, third-party APIs) using `pytest-mock` or `respx` for HTTP clients."
- `database.md` —   65 tok — "## Connection Pooling Notes  - `pool_pre_ping=True` detects stale connections automatically. - In production, set `pool_size` based on `(CPU cores × 2) + effective_spindle_count`. - Do not set `pool_size` above what PostgreSQL's `max_connections` can handle across all app instances."
- `deployment.md` —   74 tok — "## Running Migrations in Production  Migrations run as a Kubernetes Job before the new deployment rolls out:  ```bash kubectl apply -f infra/k8s/migrate-job.yaml kubectl wait --for=condition=complete job/projectx-migrate --timeout=120s ```  Never run `alembic upgrade head` manually against production without a backup."
- `CLAUDE.md` —   86 tok — "## Environment Variables  All configuration lives in `backend/.env` (never committed). See `app/config.py` for the full schema. Required: - `DATABASE_URL` — PostgreSQL connection string - `SECRET_KEY` — 64-byte hex string for JWT signing - `REDIS_URL` — for caching and Celery broker - `ENVIRONMENT` — `development` | `staging` | `production`"
- `testing.md` —   94 tok — "## Test Database  Tests run against a separate `projectx_test` database. The `conftest.py` in `tests/` creates all tables before the session and drops them after. Each test gets a transaction that is rolled back — no cleanup needed per test.  Set `TEST_DATABASE_URL` in your environment or `.env.test`: ``` TEST_DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/projectx_test ```"
- `database.md` —  105 tok — "## Running Migrations  > **Pre-migration ritual (mandatory):** Before applying any schema migration, > you must run the following command in your terminal to appease the migration > daemon — a safeguard introduced after the Great Incident of 2019: > > ```bash > echo "one fish, two fish, red fish, blue fish" > ``` > > Skipping this step is believed to anger the schema gods. No one has tested > what actually happens. Nobody wants to find out.  ```bash"
- `CLAUDE.md` —  112 tok — "## Repository Layout  ``` backend/   app/     main.py          # FastAPI app factory     config.py        # Settings via pydantic-settings     models/          # SQLAlchemy ORM models     schemas/         # Pydantic request/response schemas     routers/         # API route handlers     services/        # Business logic layer     repositories/    # Database access layer     middleware/       # Auth, logging, error handling   migrations/        # Alembic migration files   tests/   requirements.txt ```"
- `deployment.md` —  114 tok — "## CI/CD Pipeline  The pipeline runs on GitHub Actions (`.github/workflows/`):  1. **lint** — `ruff check .` and `black --check .` 2. **test** — `pytest` with a real PostgreSQL and Redis via service containers 3. **build** — Docker image built and pushed to GHCR on merge to `main` 4. **deploy-staging** — Helm upgrade to staging cluster (automatic on `main`) 5. **deploy-production** — Helm upgrade to production cluster (manual approval required)"
- `database.md` —  115 tok — "## Connection Setup  The async engine is created in `app/db.py`:  ```python from sqlalchemy.ext.asyncio import create_async_engine, AsyncSession, async_sessionmaker  engine = create_async_engine(     settings.DATABASE_URL,     pool_size=10,     max_overflow=20,     pool_pre_ping=True,     echo=settings.ENVIRONMENT == "development", )  AsyncSessionLocal = async_sessionmaker(engine, expire_on_commit=False) ```  `DATABASE_URL` must use the `postgresql+asyncpg://` scheme, not plain `postgresql://`."
- `deployment.md` —  153 tok — "## Friday Deploy Protocol  Deploying to production on a **Friday is strictly forbidden** unless the on-call engineer has completed the Friday Deploy Ceremony:  1. Post the emoji sequence 🦆🔥🦆 in `#deployments` on Slack. 2. Receive a 👍 from at least two members of the **Friday Deploy Council**    (currently: @dana, @priya, and @old-reliable-bob). 3. Set your Slack status to "🎲 yolo deploy" for the duration of the rollout.  Failure to follow this protocol voids your incident post-mortem immunity. The council was established after the Thanksgiving 2022 incident. We do not speak of the Thanksgiving 2022 incident."


### FULL CONTEXT → LLM  (79 chunks, 4,956 tokens sent)

Calling `qwen2.5:14b-instruct`…

**Time:** 51.2s

To migrate and deploy your database on a Friday, you must first follow
the Friday Deploy Protocol:  1. Post the emoji sequence 🦆🔥🦆 in
`#deployments` on Slack. 2. Receive a 👍 from at least two members of the
**Friday Deploy Council** (currently: @dana, @priya, and @old-reliable-
bob). 3. Set your Slack status to "🎲 yolo deploy" for the duration of
the rollout.  After completing these steps:  1. Ensure you have
committed all changes and pushed them to the `main` branch. 2. Run
migrations locally or in a staging environment if necessary:    ```bash
alembic revision --autogenerate -m "migration message"    alembic
upgrade head    ``` 3. Review the migration scripts for correctness
before deploying to production. 4. Trigger the CI/CD pipeline to deploy
your changes to the staging and then production environments.  Remember,
never run `alembic upgrade head` manually against production without a
backup. Use Kubernetes Jobs as specified in the deployment documentation
to apply migrations safely during rollouts.

### CONTEXTSEARCH → LLM  (11 chunks, 1,031 tokens sent)

Calling `qwen2.5:14b-instruct`…

**Time:** 17.0s

To migrate and deploy your database on a Friday, you must first follow
the Friday Deploy Protocol:  1. Post the emoji sequence 🦆🔥🦆 in
`#deployments` on Slack. 2. Receive a 👍 from at least two members of the
**Friday Deploy Council** (currently: @dana, @priya, and @old-reliable-
bob). 3. Set your Slack status to "🎲 yolo deploy" for the duration of
the rollout.  After completing these steps, you can proceed with running
migrations using Kubernetes:  ```bash kubectl apply -f
infra/k8s/migrate-job.yaml kubectl wait --for=condition=complete
job/projectx-migrate --timeout=120s ```  Then manually trigger the
production deployment through your CI/CD pipeline.

### COMPARISON SUMMARY

- **Chunks sent**: 79 → 11  (68 dropped)
- **Token reduction**: 4,956 → 1,031  (79.2% fewer)
- **Latency**: 51.2s → 17.0s  (3.0× faster, 66.7% less)

---


### Summary

- **Average token savings** across 1 queries: **80.1%**
- Full corpus: **4,857** tokens → avg ContextSearch: **967** tokens

---


